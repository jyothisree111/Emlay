{
  "name": "emlay-app",
  "version": "1.0.0",
  "private": true,
  "description": "EMLAY - Emergency Layer Fullstack Project",
  "scripts": {
    "build": "npm install --prefix website && npm run build --prefix website",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0"
  }
}
